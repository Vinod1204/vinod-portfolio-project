# Contributor Guidelines

- Run `npm run lint` and `npm run build` before committing changes.
- Keep dependency updates minimal and prefer security patches.
- Add relevant instructions to README when introducing new features.
